<resources>
    <!-- Minimal Material3 theme for Compose -->
    <style name="Theme.SecureNotes" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
